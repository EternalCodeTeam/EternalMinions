package com.eternalcode.minions.event;

/** Identifies the public entry point responsible for a minion event. */
public enum MinionEventCause {
    API,
    PLACEMENT,
    PICKUP,
    UPGRADE_PURCHASE,
    RESTORE,
    INTERNAL
}
